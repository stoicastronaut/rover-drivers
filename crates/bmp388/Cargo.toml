[package]
name = "bmp388"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Async no_std I2C driver for the Bosch BMP388 pressure sensor"
readme = "README.md"
keywords = ["embedded-hal", "bmp388", "barometer", "no-std"]
categories = ["embedded", "no-std", "hardware-support"]

[dependencies]
embedded-hal-async = "1.0.0"

[dev-dependencies]
embedded-hal = "1.0"
futures = { version = "0.3.34", features = ["executor"] }

[lints]
workspace = true
